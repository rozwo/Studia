#include <bits/stdc++.h>
using namespace std;

string mirror(string s, long long k) {
    string x="";
    for(long long i=0; i<=k; i++) {
        x+=s[i];
    }
    for(long long i=k; i>=0; i--) {
        x+=s[i];
    }
    return x;
}

int main() {
    if(n<s) {
        cout<<
    }
}